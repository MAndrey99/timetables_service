spring:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:myDb
application:
  datetime:
    format: "dd-MM-yyyy HH:mm:ss"
    zone: "Europe/Moscow"
